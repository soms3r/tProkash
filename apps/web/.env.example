# ── Web (Next.js frontend) ──

# Public URL of the web app (used for canonical links, OG tags)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# URL of the API server (used by client-side code)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Node environment
NODE_ENV=development
