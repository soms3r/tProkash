/* SDK skeleton — no API implementation yet */

export interface SdkOptions {
  baseUrl: string;
  apiKey?: string;
}

export const createSdk = (_options: SdkOptions): Record<string, never> => {
  return {};
};
