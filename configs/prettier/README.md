# Prettier

Shared Prettier configuration for all tProkash projects.

**What it contains:**
- Print width, tab width, semicolons, quotes
- Trailing commas, bracket spacing, arrow parens
- Plugin configs for Tailwind CSS class sorting
- Overrides for markdown, JSON, YAML

**Used by:** All apps, packages, and services
