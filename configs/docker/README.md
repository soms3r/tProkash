# Docker

Shared Docker configurations for tProkash services.

**What it contains:**
- Base Dockerfiles for TypeScript apps (multi-stage, production-optimized)
- Docker Compose setups for local development
- Service-specific Dockerfiles
- .dockerignore templates

**Used by:** All apps and services
