# ESLint

Shared ESLint configuration for all tProkash TypeScript projects.

**What it contains:**
- Base ESLint config with TypeScript rules
- React-specific config (for apps/web, apps/admin)
- Node-specific config (for apps/api, services)
- Import ordering rules
- Per-project overrides

**Used by:** All apps, packages, and services via `extends`
