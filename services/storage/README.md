# Storage

File storage and asset management service.

**Stack:** TypeScript, S3-compatible storage (MinIO, R2, S3)

**Purpose:** Manages uploads, downloads, and serving of static assets (book covers, author photos, dataset files).

**Depends on:** `@tprokash/types`

**Consumed by:** `apps/api`, `apps/admin`, `apps/web`
